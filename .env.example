# Sela — Environment Variables
# Copy to .env and adjust as needed
# Or run: npm run setup

# --- Core ---
ALLOWED_PHONE=         # WhatsApp phone number (no + prefix)
CLAUDE_MODEL=sonnet                # Claude model: sonnet, opus, haiku
MAX_HISTORY=20                     # Max conversation messages to keep

# --- Timezone ---
TIMEZONE=UTC                       # IANA timezone (e.g., America/New_York, Asia/Jerusalem)

# --- Persistent Mode ---
PERSISTENT_MODE=true               # Keep one Claude CLI alive, pipe messages via stdin (faster)

# --- Queue ---
MAX_CONCURRENT=2                   # Max parallel Claude CLI processes
MAX_QUEUE_PER_USER=5               # Max queued messages per user

# --- Timeouts (milliseconds) ---
CLI_TIMEOUT=300000                 # Claude CLI max execution (5 min)
COMPOSING_TIMEOUT=90000            # "typing" stuck detection (90s)
MCP_TOOL_TIMEOUT=10000             # Vestige MCP tool calls (10s)
MCP_SEARCH_TIMEOUT=5000            # Vestige MCP searches (5s)

# --- WhatsApp ---
MAX_CHUNK=4000                     # Message length limit
BATCH_DELAY=2000                   # Debounce rapid messages (ms)

# --- Quiet Hours (24h format, in configured timezone) ---
QUIET_START=23                     # Suppress cron delivery after this hour
QUIET_END=8                        # Resume cron delivery at this hour

# --- Proactive ---
PROACTIVE_INTERVAL=1800000         # Proactive check interval (30 min)

# --- Costs ---
DAILY_COST_LIMIT=5                 # Daily cost alert threshold (USD)

# --- Logging ---
LOG_LEVEL=info                     # debug, info, warn, error
LOG_RETENTION_DAYS=7               # Days to keep log files

# --- Telegram Alerts ---
TELEGRAM_BOT_TOKEN=                # Bot token from @BotFather
TELEGRAM_CHAT_ID=                  # Chat ID for alert delivery

# --- WhatsApp Alert Groups (route bot messages to dedicated groups) ---
WA_GROUP_ALERTS=                   # JID for alert messages (e.g., 120363...@g.us)
WA_GROUP_HATTRICK=                 # JID for Hattrick messages
WA_GROUP_DAILY=                    # JID for daily digest/recap

# --- Groups ---
ALLOWED_GROUPS=                    # Comma-separated group JIDs (empty = all)

# --- Dashboard ---
DASHBOARD_SECRET=                  # Password for web dashboard (empty = no auth)

# --- Claude ---
SYSTEM_PROMPT=                     # Custom system prompt (defaults to SOUL.md personality)
CLAUDE_CREDENTIALS_PATH=~/.claude/.credentials.json  # Path to Claude OAuth credentials

# --- External Tools ---
VESTIGE_MCP_PATH=                  # Path to Vestige MCP binary (defaults to ~/bin/vestige-mcp.exe)
